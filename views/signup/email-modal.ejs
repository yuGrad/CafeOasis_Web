<!-- Email Verification Modal -->
<div
	id="emailModal"
	class="hidden fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto overflow-x-hidden h-full w-full"
	onclick="closeModal(event)"
>
	<!-- 뒤 배경 -->
	<div
		class="relative top-1/3 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white"
	>
		<div class="mt-3 text-center">
			<h3 class="text-lg leading-6 font-medium text-gray-900">이메일 인증</h3>
			<div class="mt-2 px-7 py-3">
				<p class="text-sm text-gray-500">이메일 인증코드를 입력하세요.</p>
				<input
					type="text"
					id="user_code"
					class="mt-1 p-2 border-2 border-gray-300 rounded w-full"
				/>
			</div>
			<div class="items-center px-4 py-3">
				<button
					id="submitBtn"
					type="button"
					class="bg-blue-500 hover:bg-blue-dark text-white font-bold py-2 px-4 rounded"
					onclick="sendEmailVerification(event)"
				>
					확인
				</button>
			</div>
		</div>
	</div>
</div>
