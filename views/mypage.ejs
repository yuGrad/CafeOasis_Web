<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>My Page - Cafe Oasis</title>
		<link
			href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
			rel="stylesheet"
		/>
	</head>
	<body class="bg-gray-100 flex flex-col min-h-screen">
		<%- include('cafe-header.ejs') %>

		<main class="flex-grow max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
			<div class="flex justify-center space-x-4 p-4">
				<!-- Sidebar Section -->
				<section class="bg-white shadow-lg rounded-lg p-6">
					<h2 class="text-2xl font-semibold text-blue-700 mb-4">
						My Information
					</h2>
					<ul class="space-y-4">
						<li>
							<span class="text-gray-600 text-sm">Email</span>
							<p class="text-gray-900 bg-blue-50 p-2 rounded">
								<%= login.email %>
							</p>
						</li>
						<li>
							<span class="text-gray-600 text-sm">Nickname</span>
							<p class="text-gray-900 bg-blue-50 p-2 rounded">
								<%= login.nickname %>
							</p>
						</li>
						<li>
							<span class="text-gray-600 text-sm">Name</span>
							<p class="text-gray-900 bg-blue-50 p-2 rounded">
								<%= login.name %>
							</p>
						</li>
						<li>
							<span class="text-gray-600 text-sm">Mobile Phone Number</span>
							<p class="text-gray-900 bg-blue-50 p-2 rounded">
								<%= login.phone_no %>
							</p>
						</li>
					</ul>

					<!-- Navigation Buttons -->
					<div class="mt-8">
						<button
							class="w-full bg-blue-500 text-white font-semibold py-2 px-4 rounded hover:bg-blue-700 mb-4"
							onclick="loadSection('bookmarks')"
						>
							북마크한 카페
						</button>
						<button
							class="w-full bg-blue-500 text-white font-semibold py-2 px-4 rounded hover:bg-blue-700"
							onclick="loadSection('reviews')"
						>
							내가 작성한 리뷰
						</button>
					</div>
				</section>

				<!-- Main Content Section -->
				<section
					id="mainContent"
					class="col-span-2 bg-white shadow-lg rounded-lg p-6"
					style="display: none"
				>
					<h2
						class="text-2xl font-semibold text-yellow-500 mb-4"
						id="sectionTitle"
					></h2>
					<div id="sectionContent"></div>
				</section>
			</div>
		</main>

		<%- include('cafe-footer.ejs') %>
		<script src="/js/mypage.js"></script>
	</body>
</html>
